'use strict';

var React = require('react');

var LoginBox = React.createClass({
  render: function() {
    return (
      <div className="loginBox">
        This is a loginBox
      </div>
    );
  }
});

module.exports = LoginBox;
